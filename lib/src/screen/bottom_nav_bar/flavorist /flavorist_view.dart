import '../../../core_utils/export_dependency.dart';

class FlavoristView extends StatefulWidget {
  const FlavoristView({super.key});

  @override
  State<FlavoristView> createState() => _FlavoristViewState();
}

class _FlavoristViewState extends State<FlavoristView> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
